// import { useTranslation } from '@/utils';

const ProjectSection = () => {
  // const t = useTranslation('main.project');

  return (
    <div className="flex flex-col inset-0 h-screen">
    </div>
  );
};

export default ProjectSection;